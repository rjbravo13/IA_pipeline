name: retail_customer_360
version: "1.0.0"
config-version: 2

profile: retail_customer_360

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  retail_customer_360:
    staging:
      +materialized: view
      +schema: retail_dbt_staging

    intermediate:
      +materialized: table
      +schema: retail_dbt_intermediate

    marts:
      +materialized: table
      +schema: retail_dbt_mart